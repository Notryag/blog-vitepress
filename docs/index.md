---
title: 项目说明
outline: false
---

# 项目说明

这里放的是 `blog-vitepress` 仓库本身的说明，不是 `notespress` CLI 的使用手册。

## 文档入口

- [仓库说明](/docs/repository)

## 本地开发

```bash
git submodule update --init --recursive
pnpm install
pnpm dev
```

如果 `myBlog` 还没有初始化，站点仍然可以启动，但不会生成笔记导航。
